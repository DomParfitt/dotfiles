- defaults:
      link:
          create: true
          relink: true

- clean:
      ~/:
      ~/.oh-my.zsh/custom:
          recursive: true

- link:
      ~/.profile: profile
      ~/.bashrc: bashrc
      ~/.bash_logout: bash_logout
      ~/.gitconfig: gitconfig
      ~/.ssh/config: ssh/config
      ~/.vimrc: vimrc
      ~/.zprofile: zprofile
      ~/.zshrc: zshrc
      ~/bin: bin/
      ~/.config/i3: config/i3/

- create:
      - ~/projects
